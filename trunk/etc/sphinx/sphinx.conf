source dictionary
{
    type = xmlpipe2
    xmlpipe_command = wget\wget -q -O - http://localhost:12100/Home/SphinxDictionary
}

index dictionary
{
        source                  = dictionary
        path                    = var/dictionary
        docinfo                 = extern
        mlock                   = 0
        morphology              = lemmatize_ru_all
        min_word_len            = 3
        charset_type            = utf-8
        html_strip              = 1
}

source hotels
{
    type = xmlpipe2
    xmlpipe_command = wget\wget -q -O - http://localhost:12100/Home/SphinxHotels
}

index hotels
{
        source                  = hotels
        path                    = var/hotels
        docinfo                 = extern
        mlock                   = 0
        morphology              = lemmatize_ru_all
        min_word_len            = 3
        charset_type            = utf-8
        html_strip              = 1
}

source static_pages
{
    type = xmlpipe2
    xmlpipe_command = wget\wget -q -O - http://localhost:12100/Home/SphinxStaticPages
}

index static_pages
{
        source                  = static_pages
        path                    = var/static_pages
        docinfo                 = extern
        mlock                   = 0
        morphology              = lemmatize_ru_all
        min_word_len            = 3
        charset_type            = utf-8
        html_strip              = 1
}


source treatment_options
{
    type = xmlpipe2
    xmlpipe_command = wget\wget -q -O - http://localhost:12100/Home/SphinxTreatmentOptions
}

index treatment_options
{
        source                  = treatment_options
        path                    = var/treatment_options
        docinfo                 = extern
        mlock                   = 0
        morphology              = lemmatize_ru_all
        min_word_len            = 3
        charset_type            = utf-8
        html_strip              = 1
}


source resorts
{
    type = xmlpipe2
    xmlpipe_command = wget\wget -q -O - http://localhost:12100/Home/SphinxResorts
}

index resorts
{
        source                  = resorts
        path                    = var/resorts
        docinfo                 = extern
        mlock                   = 0
        morphology              = lemmatize_ru_all
        min_word_len            = 3
        charset_type            = utf-8
        html_strip              = 1
}


source resort_zones
{
    type = xmlpipe2
    xmlpipe_command = wget\wget -q -O - http://localhost:12100/Home/SphinxResortZones
}

index resort_zones
{
        source                  = resort_zones
        path                    = var/resort_zones
        docinfo                 = extern
        mlock                   = 0
        morphology              = lemmatize_ru_all
        min_word_len            = 3
        charset_type            = utf-8
        html_strip              = 1
}


source regions
{
    type = xmlpipe2
    xmlpipe_command = wget\wget -q -O - http://localhost:12100/Home/SphinxRegions
}

index regions
{
        source                  = regions
        path                    = var/regions
        docinfo                 = extern
        mlock                   = 0
        morphology              = lemmatize_ru_all
        min_word_len            = 3
        charset_type            = utf-8
        html_strip              = 1
}


source health_factors
{
    type = xmlpipe2
    xmlpipe_command = wget\wget -q -O - http://localhost:12100/Home/SphinxHealthFactors
}

index health_factors
{
        source                  = health_factors
        path                    = var/health_factors
        docinfo                 = extern
        mlock                   = 0
        morphology              = lemmatize_ru_all
        min_word_len            = 3
        charset_type            = utf-8
        html_strip              = 1
}


source deseases
{
    type = xmlpipe2
    xmlpipe_command = wget\wget -q -O - http://localhost:12100/Home/SphinxDeseases
}

index deseases
{
        source                  = deseases
        path                    = var/deseases
        docinfo                 = extern
        mlock                   = 0
        morphology              = lemmatize_ru_all
        min_word_len            = 3
        charset_type            = utf-8
        html_strip              = 1
}

source cure_profiles
{
    type = xmlpipe2
    xmlpipe_command = wget\wget -q -O - http://localhost:12100/Home/SphinxCureProfiles
}

index cure_profiles
{
        source                  = cure_profiles
        path                    = var/cure_profiles
        docinfo                 = extern
        mlock                   = 0
        morphology              = lemmatize_ru_all
        min_word_len            = 3
        charset_type            = utf-8
        html_strip              = 1
}


source countries
{
    type = xmlpipe2
    xmlpipe_command = wget\wget -q -O - http://localhost:12100/Home/SphinxCountries
}

index countries
{
        source                  = countries
        path                    = var/countries
        docinfo                 = extern
        mlock                   = 0
        morphology              = lemmatize_ru_all
        min_word_len            = 3
        charset_type            = utf-8
        html_strip              = 1
}

indexer
{
        mem_limit = 64M
}

searchd
{
        # [hostname:]port[:protocol], or /unix/socket/path to listen on
        # known protocols are 'sphinx' (SphinxAPI) and 'mysql41' (SphinxQL)
        #
        # multi-value, multiple listen points are allowed
        # optional, defaults are 9312:sphinx and 9306:mysql41, as below
        #
        # listen                        = 127.0.0.1
        # listen                        = 192.168.0.1:9312
        # listen                        = 9312
        # listen                        = /var/run/searchd.sock
        listen                  = 9312
        pid_file                = var/searchd.pid
        log             = log/searchd.log
        query_log       = log/query.log
        read_timeout    = 15
        client_timeout  = 300
        max_children    = 30
        max_matches             = 1000
        seamless_rotate         = 1
        preopen_indexes         = 1
        unlink_old              = 1
        mva_updates_pool        = 1M
        max_packet_size         = 8M
        max_filters             = 256
        max_filter_values       = 4096
        max_batch_queries       = 32
        workers                 = threads # for RT to work
#       collation_libc_locale   = ru_RU.UTF-8
        compat_sphinxql_magics=0
}

